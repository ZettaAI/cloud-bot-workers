steps:
- name: 'gcr.io/cloud-builders/docker'
  entrypoint: 'bash'
  args:
  - '-c'
  - |
    docker build -t gcr.io/$PROJECT_ID/$REPO_NAME:base .
  timeout: 600s
images:
- 'gcr.io/$PROJECT_ID/$REPO_NAME:base'

